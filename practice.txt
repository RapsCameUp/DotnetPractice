 // Check and remove any quotation marks from coordinates
                coordinates[0] = coordinates[0].Replace("\"", "");
                coordinates[1] = coordinates[1].Replace("\"", "");

                // Debug: Output the cleaned coordinate string being parsed
                Console.WriteLine($"Parsing city coordinates: '{coordinates[0]}, {coordinates[1]}'");
